const download = require('download');
const path = require('path');
const fs = require('fs-plus');

module.exports = {
  check: () => {},
  install: () => {},
  purgeBinary: () => {},

  /////////////////////////////////////////////////////////////////////////////
  // Lifecycle
  /////////////////////////////////////////////////////////////////////////////

  activate: () => {},
  deactivate: () => {}
}
